MONGO_URI=mongodb://localhost:27017/multi-region-demo
# Required for Atlas sharding/federation configuration (mocked for local dev)
ATLAS_PROJECT_ID=local-dev
# Fallback routing preference (primaryPreferred, secondary, nearest, etc.)
DEFAULT_READ_PREFERENCE=nearest
PORT=3000
